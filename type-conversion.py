num_char = len(input("What is your name ")) # Taken an integer datatypee
new_num_char = str(num_char)                # put it into parentheses of an str function
                                            # which takes an object within the parentheses and converts it to a string
                                            # which is then stored in a new variable 
                                
print("Your name has " + new_num_char + " characters, ")    # which can be used in a print statement because all the dataypes of all pieces of data, 
                                                            # that are being added together are the same datatype strings